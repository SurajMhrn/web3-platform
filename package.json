{
  "name": "web3-platform",
  "version": "1.0.0",
  "description": "Web3 Platform Monorepo",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend && npm install --prefix blockchain",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:all": "start npm run dev:backend && start npm run dev:frontend"
  }
}
